<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <%- include ('../partials/mainCss.ejs') %>
    <title>Document</title>
</head>
<body>
    <ul>
        <% foreach(let user of users){%>
        <li><%= user.id %> <%= user.name %> <%= user.password %> <%= user.created_at %> </li>
        <% }; %>
    </ul>
</body>
</html>