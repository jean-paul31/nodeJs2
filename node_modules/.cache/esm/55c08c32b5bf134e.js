let express,api,bodyParser;_b5b‍.w("express",[["default",["express"],function(v){express=v}]]);_b5b‍.w("./routes",[["default",["api"],function(v){api=v}]]);_b5b‍.w("body-parser",[["default",["bodyParser"],function(v){bodyParser=v}]]);



const app = express();
const port = 3000;
app.use(bodyParser.json({}));
app.use("/", api());
app.set('view engine', 'ejs');
app.use('/static', express.static('src/public'));

app.listen(port, () => {
                    console.log(`Exemple app listening on port ${port} !`)
                    });
app.use((err, req, res, next) => {
                                    _b5b‍.g.console.error(err.stack); res.status(500).send('oops, server error !')
                                });
app.use((req, res) => {
                        res.status(404).send("oops, you took a wrong turn !!!") 
                    });